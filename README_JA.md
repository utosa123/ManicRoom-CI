# Manic Room C execution readiness

最初に READY_JA.md を読んでください。B/C iOS buildは未実行。B target=uiの成功後、同一commitのrun IDを使いc-after-uiを実行します。

旧A2の文書は背景資料です。target=all/coreの旧説明は本版には適用せず、READY_JA.mdを優先してください。署名secret不要。リモート操作未実行。
